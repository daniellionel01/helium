diff --git a/chrome/browser/helium/deterministic/deterministic_session_service.cc b/chrome/browser/helium/deterministic/deterministic_session_service.cc
index 8b0013d..5ec61cf 100644
--- a/chrome/browser/helium/deterministic/deterministic_session_service.cc
+++ b/chrome/browser/helium/deterministic/deterministic_session_service.cc
@@ -20,8 +20,7 @@ bool DeterministicSessionService::CreateSession(std::string session_id,
 
   has_session_ = true;
   session_id_ = std::move(session_id);
-  active_step_transaction_.reset();
-  last_step_metrics_.reset();
+  orchestrator_.Reset();
   return true;
 }
 
@@ -58,77 +57,28 @@ std::optional<StepTicket> DeterministicSessionService::BeginStep() {
     return std::nullopt;
   }
 
-  active_step_transaction_ = StepTransaction{
-      .step_id = step->step_id,
-      .target_virtual_time = step->target_virtual_time,
-  };
+  if (!orchestrator_.Begin(*step)) {
+    return std::nullopt;
+  }
+
   return step;
 }
 
 bool DeterministicSessionService::MarkStepPhaseDone(StepPhase phase,
                                                     base::TimeDelta elapsed) {
-  if (!has_session_ || !active_step_transaction_.has_value()) {
-    return false;
-  }
-
-  StepTransaction& tx = active_step_transaction_.value();
-  if (IsPhaseComplete(tx, phase)) {
-    return false;
-  }
-
-  switch (phase) {
-    case StepPhase::kTick:
-      tx.tick_done = true;
-      tx.timings.tick = elapsed;
-      break;
-    case StepPhase::kRender:
-      tx.render_done = true;
-      tx.timings.render = elapsed;
-      break;
-    case StepPhase::kPresent:
-      tx.present_done = true;
-      tx.timings.present = elapsed;
-      break;
-    case StepPhase::kCapture:
-      tx.capture_done = true;
-      tx.timings.capture = elapsed;
-      break;
-    case StepPhase::kEncode:
-      tx.encode_done = true;
-      tx.timings.encode = elapsed;
-      break;
-    case StepPhase::kCommit:
-      tx.commit_done = true;
-      tx.timings.commit = elapsed;
-      break;
-  }
-
-  return true;
+  return has_session_ && orchestrator_.MarkPhaseDone(phase, elapsed);
 }
 
 bool DeterministicSessionService::CommitStep(uint64_t step_id) {
-  if (!has_session_ || !active_step_transaction_.has_value()) {
-    return false;
-  }
-
-  StepTransaction& tx = active_step_transaction_.value();
-  if (tx.step_id != step_id) {
-    return false;
-  }
-
-  if (!tx.tick_done || !tx.render_done || !tx.present_done ||
-      !tx.capture_done || !tx.encode_done) {
+  if (!has_session_ || !orchestrator_.CanCommit(step_id)) {
     return false;
   }
 
-  tx.commit_done = true;
   if (!controller_.CommitStep(step_id)) {
-    tx.commit_done = false;
-    return false;
+    return orchestrator_.FinalizeCommitFailure();
   }
 
-  last_step_metrics_ = BuildStepMetrics(tx, /*had_fault=*/false);
-  active_step_transaction_.reset();
+  orchestrator_.FinalizeCommitSuccess();
   return true;
 }
 
@@ -138,13 +88,7 @@ bool DeterministicSessionService::FailStep(FaultClass fault,
     return false;
   }
 
-  if (active_step_transaction_.has_value()) {
-    active_step_transaction_.value().commit_done = true;
-    last_step_metrics_ =
-        BuildStepMetrics(active_step_transaction_.value(), /*had_fault=*/true);
-    active_step_transaction_.reset();
-  }
-
+  orchestrator_.FailActiveTransaction();
   return controller_.Fail(fault, std::move(detail));
 }
 
diff --git a/chrome/browser/helium/deterministic/deterministic_session_service.h b/chrome/browser/helium/deterministic/deterministic_session_service.h
index 84fbbfd..d0543c6 100644
--- a/chrome/browser/helium/deterministic/deterministic_session_service.h
+++ b/chrome/browser/helium/deterministic/deterministic_session_service.h
@@ -11,7 +11,7 @@
 #include <string>
 
 #include "base/time/time.h"
-#include "chrome/browser/helium/deterministic/step_transaction_types.h"
+#include "chrome/browser/helium/deterministic/step_transaction_orchestrator.h"
 #include "chrome/browser/helium/deterministic/virtual_time_controller.h"
 
 namespace helium::deterministic {
@@ -48,15 +48,14 @@ class DeterministicSessionService {
   uint64_t last_committed_step_id() const;
   base::TimeDelta virtual_time() const;
   std::optional<StepMetrics> last_step_metrics() const {
-    return last_step_metrics_;
+    return orchestrator_.last_metrics();
   }
 
  private:
   bool has_session_ = false;
   std::optional<std::string> session_id_;
   VirtualTimeController controller_;
-  std::optional<StepTransaction> active_step_transaction_;
-  std::optional<StepMetrics> last_step_metrics_;
+  StepTransactionOrchestrator orchestrator_;
 };
 
 }  // namespace helium::deterministic
